# JobFlow Application

Core application code organized by responsibility.

## Structure

- `api/` - API endpoints and request/response handling
- `core/` - Core business logic and domain rules
- `models/` - Data models and schema definitions
- `services/` - Service layer for external integrations and orchestration
